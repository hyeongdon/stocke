# 키움증권 API 설정
# 실전투자용 키
KIWOOM_APP_KEY=your_real_app_key_here
KIWOOM_APP_SECRET=your_real_app_secret_here

# 모의투자용 키
# 신청일: 2026-01-13, 만료일: 2026-03-29
KIWOOM_MOCK_APP_KEY=Y-5kFGhoiKXPt5aQ-qnDoprjkuR4xh0biY9-hPkGBPI
KIWOOM_MOCK_APP_SECRET=mUstvGMXN0HDp_qVWNeeqZydX2lMkVpaeQSB0Fy9HlQ

KIWOOM_BASE_URL=https://openapi.kiwoom.com/v1
KIWOOM_WS_URL=wss://openapi.kiwoom.com/v1/ws
KIWOOM_WS_RECONNECT_INTERVAL=5
KIWOOM_WS_PING_INTERVAL=30

# 계좌 설정
KIWOOM_ACCOUNT_NUMBER=55009525
KIWOOM_MOCK_ACCOUNT_NUMBER=81109058
KIWOOM_USE_MOCK_ACCOUNT=true

# 데이터베이스 설정
# SQLite 사용 시:
# DATABASE_URL=sqlite:///./stock_pipeline.db
# PostgreSQL 사용 시:
# DATABASE_URL=postgresql://stocke_user:your_password@localhost:5432/stocke_db
DATABASE_URL=sqlite:///./stock_pipeline.db

# 서버 설정
HOST=0.0.0.0
PORT=8000
# 매일 KST 이 시각 이후 서버(uvicorn) 자동 종료. false면 끄기
SERVER_AUTO_SHUTDOWN_ENABLED=true
SERVER_AUTO_SHUTDOWN_TIME=19:00

# 레거시 스크리너 — 거래대금 상위 후보 수 (기본 50)
SCREENER_CANDIDATE_LIMIT=50
# 상따 — 등락률상위 풀에서 거래대금순으로 남길 후보 수 (기본 20)
SANGTTA_CANDIDATE_LIMIT=20
# 스크리너 후보 최소 등락률(%). 0이면 플러스(>0)만. 매수 최소등락보다 약간 낮게(예: 3.5→3.3)
SCREENER_MIN_CHANGE_RATE=3.3
# 스크리너 후보 과열 컷(%). 이 이상이면 편입 제외. 0이면 상한 미적용
SCREENER_MAX_CHANGE_RATE=15
# 스크리너 후보 최소 당일 거래대금(억원). 0이면 하한 미적용
SCREENER_MIN_TRADE_AMOUNT_EOK=20

# 조건식 감시 설정
# CONDITION_CHECK_INTERVAL: (레거시) 신호 중복 방지 등 — 주기 검색과 별개
CONDITION_CHECK_INTERVAL=60
# 조건식 주기 검색(CNSRREQ) — 기본 OFF (API 절감). true 시 /monitoring/start 와 동일 효과
CONDITION_MONITOR_AUTO_ENABLED=false
# 주기 검색 간격(초). 최소 60초
CONDITION_MONITOR_INTERVAL=600

# 로깅 설정
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# KRX 추가 휴장일 (쉼표 구분 YYYY-MM-DD) — DB 외 env 임시 추가용
# 대시보드 「자동매매」탭 → 거래소 휴장일 에서 DB 관리 권장
# KRX_EXTRA_HOLIDAYS=2026-06-03

# WebSocket 서버 설정
WEBSOCKET_PING_INTERVAL=20
WEBSOCKET_PING_TIMEOUT=20

# 데이터 내보내기 설정
EXPORT_DIR=./exports
MAX_EXPORT_ROWS=10000

# 공공데이터포털 관세청 수출입 OpenAPI (지표 > 수출입 섹터)
# data.go.kr 에서 발급 — Itemtrade는 별도 활용신청 필요할 수 있음
DATA_GO_KR_SERVICE_KEY=your_service_key_here

# 텔레그램 알림 설정
# 봇 토큰(@BotFather)과 채팅 ID (필수)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here
# 아침 스케줄(ensure_server_running) 서버 신규 기동 시에도 위 토큰으로 알림 전송
# 자동매매 매수/매도 체결 시에도 동일 토큰으로 알림 전송
# 알림 대상 조건식 이름 필터(쉼표 구분). 비우면 모든 조건식 대상.
TELEGRAM_ALERT_CONDITION_NAMES=
# 반복 실행 주기(초)
TELEGRAM_ALERT_INTERVAL=600
# 조건식별 표시 최대 종목 수
TELEGRAM_ALERT_MAX_STOCKS=20
# true: 조건식·매매 알림은 거래일 장중(09:00~15:30)만 (휴장·주말·장외 스킵)
TELEGRAM_ALERT_MARKET_HOURS_ONLY=true